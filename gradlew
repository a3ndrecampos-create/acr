#!/usr/bin/env sh
# Placeholder wrapper script - abra o projeto no Android Studio e deixe o
# próprio Studio gerar o gradle-wrapper.jar/gradlew completo (Ficheiro >
# Sync Project with Gradle Files), ou rode `gradle wrapper` localmente
# se tiver o Gradle instalado.
echo "Abra este projeto no Android Studio para gerar o Gradle Wrapper automaticamente."
